# Anthropic API Key (for production, proxy through backend instead)
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here

# Backend URL (if using a proxy server)
VITE_API_BASE_URL=http://localhost:8000

# App config
VITE_APP_NAME=NeuralCode
VITE_APP_VERSION=1.0.0
